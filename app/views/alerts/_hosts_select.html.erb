<%= select_tag "alert[host_ids]", options_for_select(Host.where(:service_ids => service_id).collect{|h| [h.name, h.id]}, @alert.host_ids), { :multiple => true, :size => 5, :class => "form-control" } %>
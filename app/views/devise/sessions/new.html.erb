<% content_for :title, t("devise.session.new.title") %>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-group">
    <%= f.email_field :email, :autocomplete => "off", :class => "form-control", :placeholder => t("devise.session.new.email_placeholder"), :required => :true %>
  </div>

  <div class="form-group">
    <%= f.password_field :password, autocomplete: "off", :class => "form-control", :placeholder => t("devise.session.new.password_placeholder"), :required => :true %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="checkbox">
      <label>
        <%= f.check_box :remember_me %> <%= t("devise.session.new.remember_me") %>
      </label>
    </div>
  <% end -%>

  <div class="form-group">
    <%= f.submit t("devise.session.new.log_in"), :class => "btn btn-primary btn-block" %>
  </div>
<% end %>
<%- if devise_mapping.recoverable? %>
  <%= link_to t("devise.session.new.forgot_password"), new_password_path(resource_name) %>
<% end -%>
